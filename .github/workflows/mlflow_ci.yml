name: Workflow CI/CD Model ML (Ultimate Fix)
on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    name: 🚀 Build, Test, and Deploy ML Model
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      MLFLOW_TRACKING_URI: https://dagshub.com/${{ secrets.DAGSHUB_USERNAME }}/smsml_tema.mlflow
      MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
      MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_TOKEN }}

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Conda with environment from file
        uses: conda-incubator/setup-miniconda@v3
        with:
          environment-file: MLProject/conda.yaml
          activate-environment: mlflow-tensorflow-env
          auto-activate-base: false

      - name: 🏃 Run MLflow project to train model
        shell: bash -l {0}
        run: |
          cd MLProject
          mlflow run . --experiment-id 0 -P epochs=5

      - name: 🆔 Get MLflow run_id from file
        id: get_run_id
        shell: bash -l {0}
        run: |
          RUN_ID_FILE="MLProject/run_id.txt"
          if [ ! -f "$RUN_ID_FILE" ]; then echo "❌ File run_id.txt not found!"; exit 1; fi
          RUN_ID=$(cat $RUN_ID_FILE)
          if [[ -z "$RUN_ID" ]]; then echo "❌ Failed to read run ID from file!"; exit 1; fi
          echo "✅ Run ID from file: $RUN_ID"
          echo "RUN_ID=$RUN_ID" >> $GITHUB_ENV

      # =================================================================
      # === PERUBAHAN KUNCI: UNDUH SEMUA ARTEFAK ========================
      # =================================================================
      - name: 📥 Download ALL artifacts for Docker build
        shell: bash -l {0}
        run: |
          echo "Downloading ALL artifacts from run ID: $RUN_ID into ./artifacts/"
          # Hapus opsi --artifact-path untuk mengunduh semuanya
          mlflow artifacts download --run-id $RUN_ID -d ./artifacts
          echo "✅ All artifacts downloaded. Verifying contents:"
          ls -R ./artifacts

      - name: 🐳 Build Docker Image using custom Dockerfile
        run: |
          echo "Building Docker image using custom Dockerfile..."
          docker build -t smsml-model -f Dockerfile .

      - name: 🔐 Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🚢 Push Docker Image to Docker Hub
        run: |
          docker tag smsml-model:latest ${{ secrets.DOCKERHUB_USERNAME }}/smsml-model:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/smsml-model:latest

      - name: 🔓 Logout from Docker Hub
        run: docker logout

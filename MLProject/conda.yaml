name: rice-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - tensorflow
      - mlflow
      - dagshub
      - matplotlib
      - seaborn
      - scikit-learn
